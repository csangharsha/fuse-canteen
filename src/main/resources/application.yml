server:
  port: 8082

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fuse_canteen?createDatabaseIfNotExist=true&useUnicode=yes&characterEncoding=UTF-8
    username: root
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: false

app:
  basePackage: "np.com.csangharsha.fusecanteen"
  basePath: "/api.*"

jwt:
  accessToken:
    secretKey: 5ebe2294ecd0e0f08eab7690d2a6ee69